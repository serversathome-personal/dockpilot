# DockPilot Environment Configuration

# Server Configuration
PORT=3000
NODE_ENV=production

# User/Group Permissions (TrueNAS Compatible)
# Set these to match your host system's user/group IDs
# Find your UID: id -u
# Find your GID: id -g
PUID=1000
PGID=1000

# Docker Configuration
DOCKER_HOST=unix:///var/run/docker.sock
DOCKER_SOCKET=/var/run/docker.sock

# Stacks Directory
# This is the directory inside the container where docker-compose files are stored
STACKS_DIR=/stacks

# Data Directory
# Directory for persistent application data (configs, update history, etc.)
DATA_DIR=/data

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=info
LOG_FILE=/data/logs/app.log

# WebSocket Configuration
WS_HEARTBEAT_INTERVAL=30000

# Optional: Authentication (Future Feature)
# AUTH_ENABLED=false
# JWT_SECRET=your-secret-key-here

# TrueNAS Specific Notes:
# 1. Find the UID/GID of your TrueNAS dataset owner:
#    - SSH into TrueNAS
#    - Run: ls -ln /mnt/your-pool/your-dataset
#    - Use the numeric UID and GID shown
# 2. Set PUID and PGID to match those values
# 3. The stacks directory will be automatically owned by this user
